/** @format */

import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  button: {
    flex: 1,
    justifyContent: "center",
    alignItems: "center",
    borderRadius: 4,
  },

  text: {
    paddingVertical: 8,
    color: "#fff",
    fontSize: 16,
    fontFamily: "Nunito-Bold-600",
  },

  activeButtonColor: {
    backgroundColor: "#272430",
  },
});
