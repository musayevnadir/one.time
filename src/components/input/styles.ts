/** @format */

import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  inputContainer: {
    backgroundColor: "#CCC2FE",
    flexDirection: "row",
    alignItems: "center",
    borderRadius: 20,
    borderWidth: 0,
    paddingVertical: 8,
    paddingLeft: 15,
    gap: 10,
  },
});
