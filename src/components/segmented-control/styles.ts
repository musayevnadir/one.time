/** @format */

import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flexDirection: "row",
    backgroundColor: "#3C1F7B",
    borderRadius: 4,
    paddingVertical: 4,
    paddingHorizontal: 4,
  },
});
